# Práctica-Ansible-Vagrant

```
vagrant up
```

* Despues, cambia las ips de mantenimiento en `ansible/hosts` por las ips asignadas a eth0 en cada una de las máquinas creadas.

* Una vez hecho, dentro de la carpeta ansible, se ejectuta el playbook:
  
```
ansible-playbook site-yaml
```
